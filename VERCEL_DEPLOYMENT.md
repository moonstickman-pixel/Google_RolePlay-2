# نشر الموقع على Vercel

## الخطوات

### 1. تحميل الكود إلى GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2. النشر على Vercel

1. اذهب إلى [Vercel](https://vercel.com)
2. قم بتسجيل الدخول باستخدام GitHub
3. اضغط على "New Project"
4. اختر المستودع الخاص بك
5. Vercel سيكتشف إعدادات Vite تلقائياً
6. اضغط على "Deploy"

### 3. إضافة متغيرات البيئة (اختياري)

إذا كنت تستخدم API keys:
1. اذهب إلى Project Settings
2. اضغط على "Environment Variables"
3. أضف المتغيرات:
   - `GEMINI_API_KEY` (إذا كنت تستخدم Gemini)

### ملاحظات مهمة

- الموقع يعمل بشكل كامل بدون أخطاء
- جميع التبعيات محدثة وجاهزة
- التكوينات محسّنة للنشر على Vercel

## التحقق من البناء محلياً

```bash
npm install
npm run build
npm run preview
```

تم البناء بنجاح! 🚀
